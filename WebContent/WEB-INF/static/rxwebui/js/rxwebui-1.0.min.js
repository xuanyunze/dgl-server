SessionInfo = {
	init:function(dataurl,msg){
		var session = {};
		session.userid = msg.userId;
		session.username = msg.userName;
		session.usertype = msg.userType;
		session.orgid = msg.orgId;
		session.keystr = msg.keyString;
		session.logintime = msg.loginTime;
		session.stat = msg.stat;
		session.dataurl = dataurl;
		
		session.refresh = function(dataurl){
			$.ajax({
				type : "GET",
				url : dataurl,
				data : "",
				dataType : "json",
				success : function(msg){
					session.userid = msg.mObject.userId;
					session.username = msg.mObject.userName;
					session.usertype = msg.mObject.userType;
					sesison.orgid = msg.mObject.orgId;
					session.keystr = msg.mObject.keyString;
					session.logintime = msg.mObject.loginTime;
					session.stat = msg.mObject.stat;
				}
			});
		};
		return session;
	}
};

var Calendar = {
	init:function(){
		var calendar = {};
		var date = new Date();
		calendar.year = date.getFullYear();
		calendar.month = date.getMonth() + 1;
		calendar.date = date.getDate();
		calendar.day = new Array("星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六")[date.getDay()];
		calendar.hour = date.getHours() < 10 ? "0" + date.getHours() : date.getHours();
		calendar.minute = date.getMinutes() < 10 ? "0" + date.getMinutes() : date.getMinutes();
		calendar.second = date.getSeconds() < 10 ? "0" + date.getSeconds() : date.getSeconds();
		calendar.getDateText = function(){
			return calendar.year+"年"+calendar.month+"月"+calendar.date+"日";
		};
		calendar.getDayText = function(){
			return calendar.day;
		};
		return calendar;
	}
};
